import { ProductDetails } from '../second-row-content/productDetails.model';
export class ThirdRowModel {
    title: string;
    description: string;
    productDetails: [ProductDetails];
    publish: boolean;
}
